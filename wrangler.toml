# Publicare pe Cloudflare, cu poartă de parolă (fără Zero Trust, fără card).
#
#   npx wrangler secret put FRYDAY_PAROLA     # o dată: introduci parola echipei
#   bash scripts/publica-cloudflare.sh        # construiește și publică
#
# Fără worker (aplicație deschisă, doar dacă build-ul e fără date):
#   npx wrangler pages deploy dist --project-name fryday-fi
name = "fryday-fi"
main = "worker/index.ts"
compatibility_date = "2026-08-01"

[assets]
directory = "./dist"
binding = "ASSETS"
